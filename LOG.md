# Codebase audit log

First-party audit ledger. `IDEAS.md` and `vendor/microhs/` are intentionally out of scope.

| Date | Category | Area | Change | Evidence / why | Verification |
|---|---|---|---|---|---|
| 2026-07-10 | slop | `crates/minimal` | Removed superseded FPC helpers and unused census/work-item remnants. | `cargo test` reported 18 warnings; searches found no live callers/readers. | Warning-free `cargo test --manifest-path crates/minimal/Cargo.toml`; production build and release check pass. |
| 2026-07-10 | slop | `crates/reduce` | Removed the unused `TAG_FREE` binding; wire tag 2 remains documented and pass-through handling is unchanged. | `cargo test` reported the constant as dead; decoding never branches on free nodes. | Warning-free `cargo test --manifest-path crates/reduce/Cargo.toml`; production build and release check pass. |
| 2026-07-10 | docs | architecture docs and module headers | Replaced dead `PLAN.md`, `layout.ts`, and `layout3d.ts` pointers with their current ADR/module locations. | The referenced files were deleted or split; live paths are `docs/adr/` and `src/core/layouts/`. | `npm run typecheck`, `npm run build`, and `npm run check:release` pass. |
| 2026-07-10 | simplify | `crates/minimal` | Replaced three comparator/index-shape remnants with the direct `sort_by_key`/`values` forms. | `clippy::all` found them; unlike its other loop suggestions, these do not encode reduction stack or enumeration indices. | `cargo test` passes; those three lints are gone from `clippy::all`. |
| 2026-07-10 | docs | ADRs and recorder contract | Updated deleted reader/modal/hook names, current optimization defaults, kernel status, and the shipped 3D recorder path. | Source searches showed the prose described APIs and UI states that no longer exist; historical specs remain untouched. | Dead-reference scan is clean; `npm run build` passes. |
| 2026-07-10 | slop | layout, sound, and recorder modules | Removed three exports with no external consumer, including the orphaned `resolveAutoLayout` recorder helper. | Import-AST scan plus history showed the recorder switched to direct Auto → H-tree prefill after the helper landed. | Repeat export scan is clean; `npm run build` passes. |
| 2026-07-10 | correctness | `core/refold` parser boundary | Reject empty egg expressions and trailing tokens instead of silently constructing/accepting a prefix. | Direct probes showed `fromEgg("")` produced an empty free variable and `fromEgg("iota junk")` returned ι; permalink decoding promises malformed input returns `null`. | Malformed-input + round-trip smoke, `npm run build`, and 66-law release check pass. |
| 2026-07-10 | robustness | DuckDB and MP4 adapters | Close prepared statements when a query rejects, and cancel a partially-started Mediabunny output when encoder setup fails. | Both adapters released resources only after successful setup/query; their callers cannot clean up objects that were never returned. | `npm run build` passes; Playwright recorder smoke renders 15/15 frames and 2/2 steps. |
| 2026-07-10 | efficiency | recorder modal lifecycle | Cancel the preview debounce and invalidate an in-flight preview when the modal closes. | Planning already had symmetric close-time cancellation; preview work could still start or publish after the modal became hidden. | `npm run build` passes. |
| 2026-07-10 | refactor | re-folder loader | Memoize and return the in-flight WASM load promise instead of a parallel boolean flag. | Concurrent callers previously returned before boot loading finished, so `await ensureRefolder()` did not reliably mean ready; the other lazy loaders already use promise memoization. | `npm run build` and the Playwright boot smoke pass. |
| 2026-07-10 | robustness | Turbo loader | Clear the in-flight promise after settlement while retaining a successfully loaded module. | A failed preload was cached forever as `Promise<null>`, preventing an explicit later Turbo toggle from retrying a transient failure. | `npm run build` and the full TS↔WASM parity suite pass (1,091 cases + 8 MicroHs dumps). |
| 2026-07-10 | correctness | OpenGraph generator | Point the generator at the current layout barrel after the layout module split. | The script still imported deleted `src/core/layout`, so it no longer type-checked or ran. | All TypeScript scripts type-check together; `npm run build` passes. |
